version: '3.8'

services:
  neo4j:
    image: neo4j:5.15.0
    container_name: reframing_neo4j
    ports:
      - "7474:7474" # HTTP (Browser)
      - "7687:7687" # Bolt (Driver)
    environment:
      - NEO4J_AUTH=neo4j/password
      # Enable APOC procedures if needed explicitly, though core is often enough
      - NEO4J_PLUGINS=["apoc"] 
    volumes:
      - ./data/neo4j_data:/data
      - ./data/neo4j_plugins:/plugins
    restart: unless-stopped
    healthcheck:
        test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider localhost:7474 || exit 1"]
        interval: 10s
        timeout: 5s
        retries: 5
